import type { FeeKey } from "../../types/loan";

export default function FeeField({ label, value, onChange }: { feeKey: FeeKey; label: string; baseFee: { type: "flat" | "percentage"; value: number; includeUpfront: boolean }; value: { type: "flat" | "percentage"; value: number; includeUpfront: boolean }; onChange: (value: { type: "flat" | "percentage"; value: number; includeUpfront: boolean }) => void }) {
  return <div className="rounded-xl border border-slate-200 p-3"><div className="font-extrabold text-velo-900 text-xs mb-2">{label}</div><div className="flex items-center gap-2"><select value={value.type} onChange={(e) => onChange({ ...value, type: e.target.value as "flat" | "percentage" })} className="velo-input !py-2 text-xs font-bold w-28"><option value="flat">Flat (₦)</option><option value="percentage">Percentage (%)</option></select><input type="number" min={0} step={value.type === "percentage" ? 0.1 : 500} value={value.value} onChange={(e) => onChange({ ...value, value: Number(e.target.value) || 0 })} className="velo-input !py-2 text-sm font-bold flex-1" /><span className="text-xs font-bold text-slate-500">{value.type === "flat" ? "₦" : "%"}</span></div><label className="mt-2 flex items-center gap-2 text-[11px] text-slate-600"><input type="checkbox" checked={value.includeUpfront} onChange={(e) => onChange({ ...value, includeUpfront: e.target.checked })} className="accent-velo-500" /> Include in upfront total repayment</label></div>;
}
